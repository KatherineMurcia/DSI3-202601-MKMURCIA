import { Component } from '@angular/core';
import { Button } from '../../atoms/button/button';

@Component({
  selector: 'app-about-buttons',
  imports: [Button],
  templateUrl: './about-buttons.html',
  styleUrl: './about-buttons.css',
})
export class AboutButtons {}
